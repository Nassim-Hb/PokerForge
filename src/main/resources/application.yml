spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pokerforge
    username: pokerforge_user
    password: 212Xx3620.

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
    server:
      port: 8080